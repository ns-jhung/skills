# Stork-Debian: Python 3.8 → 3.12

Reference PR: https://github.com/netSkope/ep-sample-build-app/pull/38

This is the **major** Stork migration. The build system itself is changing — `pythonpkg-py3.mk` is being replaced with `conda-build.mk`, and the Makefile gets reorganized. Expect significant dependency churn since py38 packages may not be py312-compatible.

## Files to change

- `Makefile` (significant restructuring)
- `requirements.txt` (regenerated via `uv pip compile`)
- `app-env-py312.yaml` (generated by `conda-env-gen`; delete old `app-env-py38.yaml`)
- `tox.ini` (significant restructuring)
- `Dockerfile-2004`
- `Dockerfile-2004-fips`
- `pyproject.toml` (create if missing — required for PEP 517 wheel build under py312)

## Step 1 — Makefile (significant restructure)

Six things change:
1. Move `STORK_COMPONENT_NAME`, `FIRST_LOCAL_COMPONENT_TARGETS`, and the `stork-build.mk` include **to the top** of the file (better organization).
2. Add Python 3.12 support variables: `PYTHON_VERSION ?= py312`, `COMPONENT_DIR`, and `APP_ENV_YML` referencing the versioned conda env.
3. Replace direct `pythonpkg-py3.mk` include with **conditional `conda-build.mk`** logic (errors helpfully if missing).
4. Remove the old `pythonpkg-py3.mk` include line.
5. Add a `deb-install-package-items` target as a placeholder hook.
6. Add `export PIP_INDEX_URL=...org-external-pypi/simple` (and `SETUPTOOLS_USE_DISTUTILS=local`) so the PEP 517 isolated build env can resolve `setuptools>=70.0.0`. See "PIP_INDEX_URL note" below.

```diff
+
+STORK_COMPONENT_NAME = dyncleanup
+FIRST_LOCAL_COMPONENT_TARGETS = publish
+include $(NS_BUILD_DIR)/compile/stork-build.mk
+
 WHAT         = "Uses nstenantcleanup for dyn deprovisioning"
+PYTHON_VERSION          ?= py312
+COMPONENT_DIR            = $(NS_BUILD_DIR)/components/stork-debian-py38
+APP_ENV_YML              = $(COMPONENT_DIR)/app-env-$(PYTHON_VERSION).yaml
 SERVICE_NAME = "nsdyncleanup"
 DEB_PKG_NAME = "nsdyncleanup"
 REQUIRE_NETSKOPE_COMMON = "no"
@@
 PYTHON_LOCAL_BINARY_PATH =

 VIRTUALENV_PYPI_TYPE=unbounded

+export PIP_INDEX_URL=https://artifactory-rd.netskope.io/artifactory/api/pypi/org-external-pypi/simple
+export SETUPTOOLS_USE_DISTUTILS=local
+
-STORK_COMPONENT_NAME = dyncleanup
-FIRST_LOCAL_COMPONENT_TARGETS = publish
-include $(NS_BUILD_DIR)/compile/stork-build.mk
+## Do not modify the following vars or include logic ##
+MK_DIR             = $(NS_BUILD_DIR)/compile
+PYTHONPKG_MK_FILE = $(MK_DIR)/conda-build.mk
+HAVE_MK_PYTHONPKG = $(shell test -r $(PYTHONPKG_MK_FILE) && echo yes)
+
+ifeq ($(strip $(HAVE_MK_PYTHONPKG)),yes)
+       include $(PYTHONPKG_MK_FILE)
+else
+$(error Cannot find $(PYTHONPKG_MK_FILE). Make sure you are building from within the project top-level directory.)
+endif
+
+

 # Generally used when creating a python package based on netskope common.ns
 # code. Format is:
@@
 #  OVERLAY_FILES = $(NS_BUILD_DIR)/common/netskope/log/*.py:src/netskope/log
 #

-include $(NS_BUILD_DIR)/compile/pythonpkg-py3.mk
+# Add commands to this target to install files into your debian package. The
+# root location for package building can be referenced using the PKG_BUILD_PATH
+# variable.
+#
+# Example:
+#
+# mkdir -p $(PKG_BUILD_PATH)/opt/ns/cfg && \
+#     cp config/my_config.cfg $(PKG_BUILD_PATH)/opt/ns/cfg
+#
+deb-install-package-items::
+       @echo "(no extra files to install)"
```

Adjust component name (`dyncleanup`) and `COMPONENT_DIR` to match the user's component.

**PIP_INDEX_URL note.** Without the `export PIP_INDEX_URL=...` line, the Drone wheel build fails:

```
ERROR: Could not find a version that satisfies the requirement setuptools>=70.0.0
       (from versions: 44.1.1, 57.4.0, 59.2.0)
Looking in indexes: https://artifactory-rd.netskope.io/artifactory/api/pypi/netskope-py38/simple
```

The Drone builder image's `/etc/pip.conf` points at the legacy `netskope-py38` index. Local `pip-unbounded.conf` is copied into the conda builder env, but `python -m build -w` spawns a PEP 517 isolated venv at `/tmp/build-env-XXX/` that does **not** inherit it — pip falls through to `/etc/pip.conf`. Only a process-level env var (`PIP_INDEX_URL`) overrides the file-level config for that ephemeral venv. Reference: `dp-py-apps/ipsconfigmanager/Makefile` (commit `52fcd3fc25b`, ENG-986779).

## Step 2 — Print this `uv pip compile` command for the user

```bash
docker run \
  --mount type=bind,source="$(pwd)",target=/ep-tools/uv \
  artifactory-rd.netskope.io/org-tool-release-docker/ns-builder/develop-uv-python3.12-ubuntu2004-current:26.84.3019 \
  uv pip compile \
  --python-version=3.12 \
  --emit-index-url \
  --generate-hashes \
  -o requirements.txt \
  requirements.in
```

Substitute `$(pwd)` with the absolute path to the component directory.

## Step 3 — Print this `conda-env-gen` command for the user

```bash
docker run \
  --rm \
  --mount type=bind,source="/path/to/repo",target=/cenv/repo \
  artifactory-rd.netskope.io/org-tool-release-docker/ns-utilities/conda-env-gen-ubuntu2404-current:26.85.3004 \
  -p 3.12 \
  -r repo/components/<component-name>/requirements.txt
```

Substitute `/path/to/repo` and `<component-name>`. After the user runs it, **delete `app-env-py38.yaml`**.

## Step 4 — tox.ini

Five changes:
- `envlist`: `py38` → `py312`
- `requires`: add `tox-conda` (new)
- `install_command`: simplify path from `{env:NS_BUILD_DIR}/build/python_packages` to `{toxinidir}/dist`
- Remove old `deps =` block (`--no-deps` and `-Urrequirements.txt`)
- Add `conda_env = app-env-py312.yaml` and `conda_channels = conda-forge`

```diff
 [tox]
-envlist = py38
+envlist = py312
+requires = tox-conda

 [testenv]
-install_command = pip install --find-links file://{env:NS_BUILD_DIR}/build/python_packages {opts} {packages}
-deps=
-    --no-deps
-    -Urrequirements.txt
+install_command =
+    pip install \
+    --find-links file://{toxinidir}/dist \
+    {opts} {packages}
+
+conda_env = app-env-py312.yaml
+conda_channels=
+    conda-forge
```

## Step 5 — Dockerfile-2004 (commercial)

Four things:
- Base image: `cloud-python3-2004:25.86.204` → `cloud-python3-py312-2004:26.84.267`
- Remove `python3.8-dev` from apt install (the new base image has Python 3.12 baked in)
- Package install: `.tar.gz` → `-py3-none-any.whl` (wheel format)
- pip command: explicit `python3.12 -m pip` instead of bare `pip`

```diff
 # syntax = docker/dockerfile:experimental
-FROM artifactory-rd.netskope.io/org-base-docker/cloud-python3-2004:25.86.204
+FROM artifactory-rd.netskope.io/org-base-docker/cloud-python3-py312-2004:26.84.267
 ARG PyPiVersion

 ARG HomeDir
@@
 RUN /usr/sbin/usermod -aG sudo nsadmin


-RUN apt-get update && apt-get -y install libssl-dev libffi-dev gcc g++ python3.8-dev mariadb-client-core-10.4
+RUN apt-get update && apt-get -y install libssl-dev libffi-dev gcc g++  mariadb-client-core-10.4
 # Install requirements for dyncleanup
 RUN --mount=type=bind,target=/src_tree \
         pip install --no-deps -r /src_tree/requirements.txt
-RUN --mount=type=bind,target=/src_tree pip install /src_tree/dist/nsdyncleanup-${PyPiVersion}.tar.gz
+RUN --mount=type=bind,target=/src_tree python3.12 -m pip install /src_tree/dist/nsdyncleanup-${PyPiVersion}-py3-none-any.whl
```

Substitute `nsdyncleanup` with the user's package name. Find the latest base-image tag at https://tool-ep-pokeball.netskope.io.

## Step 6 — Dockerfile-2004-fips (compliance variant)

Apply the same edits as Step 5. Don't skip this file.

## Step 7 — pyproject.toml (create if missing)

Components with only `setup.py` and no `pyproject.toml` will fail the Drone wheel build under py312 with:

```
ERROR Backend 'setuptools.build_meta:__legacy__' is not available.
Failed to build wheel for /drone/src/dp-py-apps/<component>
```

Root cause: `python -m build` in the py312 conda env (newer `build` + `setuptools` 70+) no longer falls back to the legacy backend alias when no `pyproject.toml` is present.

Fix: create a 3-line `pyproject.toml` next to `setup.py`:

```toml
[build-system]
requires = ["setuptools>=70.0.0", "wheel"]
build-backend = "setuptools.build_meta"
```

Reference: `dp-py-apps/ipsconfigmanager/pyproject.toml` is identical and was added during its py312 migration.

If `pyproject.toml` already exists, leave it alone — only create when missing.

## Critical notes

✅ DO:
- Carefully review **all** dependencies for py312 compatibility — py38 packages often need bumping
- Test thoroughly before committing
- Update **both** Dockerfiles
- Run the full test suite
- Be aggressive cleaning up legacy py38 packages from `requirements.in`
- Create `pyproject.toml` if it doesn't exist (otherwise wheel build will fail in Drone)

❌ DON'T:
- Use `latest` tag
- Assume py38 packages work on py312
- Skip testing after this major upgrade
- Mix Python versions in the same build
- Forget to delete `app-env-py38.yaml`
